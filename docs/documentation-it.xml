<h1 id="swalstrap-for-bootstrap-4">Swalstrap for Bootstrap 4</h1>
<p>Un&#39;alternativa a Swetalert e Bootbox, basata sulle componenti Modal e Toast di Bootstrap 4. Cerchi una versione per Bootstrap 5? <a href="https://github.com/magicbruno/swalstrap5">Vai qui</a>.</p>
<h2 id="per-iniziare">Per iniziare</h2>
<p>Carica Swalstrap da CDN:</p>
<pre><code>&lt;script src=&quot;https://cdn.jsdelivr.net/npm/@magicbruno/swalstrap4@1.0.0/dist/js/swalstrap_all.min.js&quot;&gt;&lt;/script&gt;
</code></pre>
<p>swalstrap_all.js caricherà automaticamente il foglio di stile necessario e creerà un&#39;istanza predefinita della classe Swalstrap chiamata Swal (verranno creati anche gli alias: swal, Sweelelert e sweelelert).</p>
<p>Per utilizzare Swalstrap devi applicare il metodo fire() all&#39;istanza creata:</p>
<pre><code>&lt;script&gt;
    Swal.fire(&#39;Wanderful!&#39;,&#39;Swalstrap is working!&#39;,&#39;success&#39;)
&lt;/script&gt;
</code></pre>
<p>Se preferisci caricare separatamente il foglio di stile (o caricarne uno personalizzato)</p>
<pre><code>&lt;link rel=&quot;stylesheet&quot; href=&quot;https://cdn.jsdelivr.net/npm/@magicbruno/swalstrap4@1.0.0/dist/css/swalstrap.min.css&quot;&gt;
</code></pre>
<p>devi usare swalstrap.js:</p>
<pre><code>&lt;script src=&quot;https://cdn.jsdelivr.net/npm/@magicbruno/swalstrap4@1.0.0/dist/js/swalstrap.min.js&quot;&gt;&lt;/script&gt;
</code></pre>
<p>In questo caso è a tuo carico creare almeno un&#39;istanza di swalstrap:</p>
<pre><code>&lt;script&gt;
    // Create an instance 
    const mySwal = new Swalstrap();
    // Then use it for all your popups
    mySwal.fire(&#39;Wanderful!&#39;,&#39;Swalstrap is working!&#39;,&#39;success&#39;);
&lt;/script&gt;
</code></pre>
<h2 id="download">Download</h2>
<p>In alternativa è possibile installare il pacchetto tramite npm:</p>
<pre><code>npm install @magicbruno/swalstrap4@1.0.0
</code></pre>
<p>clonare il repository git:</p>
<pre><code>git clone https://github.com/magicbruno/SwalStrap4.git
</code></pre>
<p>o <a href="https://github.com/magicbruno/SwalStrap4/archive/refs/heads/main.zip">scaricalo</a>.</p>
<blockquote>
<h3 id="avvertimento">Avvertimento</h3>
<p>Swalstrap è ispirato a Sweelelert, non è un clone della componente. Le caratteristiche sono riprodotte non copiate.
E quindi ci sono differenze. Controlla la documentazione e gli esempi.</p>
</blockquote>
<ul>
<li><a href="https://magicbruno.github.io/SwalStrap4/api-it.html">Documentazione</a>.</li>
<li><a href="https://magicbruno.github.io/SwalStrap4/basic-examples.html">Esempi</a>.</li>
<li><a href="https://magicbruno.github.io/SwalStrap4/custumization.html">Esempio di personalizzazione</a>.</li>
</ul>
